# AGENTS.md — mzomSite

## قاعدة ملزمة: الاستشارة ≠ التنفيذ (توجيه الكاتب 2026-09-21)

- ما يطلبه الكاتب **استشارةً** يبقى استشارة: لا تعديل ملفات، لا `python3 build.py`، لا `git commit`، ولا `git push` إلا بطلب تنفيذ صريح («نفّذ»، «ابنِ»، «احفظ»، «انشر»…).
- الاستشارة تعني: تحليل وخطة ونصوص مقترحة تُعرض في الجلسة فقط — والتنفيذ يبدأ بعد موافقة صريحة.
- خلفية: في 2026-09-21 طلب الكاتب استشارة عن توحيد خدماته فنفّذ الوكيل «طبقة الخدمات» كاملة والتزم بها محليًا دون طلب (الالتزام 81d4e39 محلي، غير مدفوع — يُبقى أو يُلغى بقرار الكاتب).

## Session protocol (automatic)

1. **At the start of every session:** read `PLAN.md` first to know the current state of work. Do not re-inspect or re-verify work that is already marked as done.
2. **While working:** use this map (project structure below) instead of random searching.
3. **Before finishing any task:** update automatically, without being asked:
   - `AGENTS.md` — if the structure/architecture changed.
   - `PLAN.md` — if the work status changed.

## Project map

- **Type:** static website hosted on **GitHub Pages** (repo: Motaz3d/mzomSite, branch `main`, path `/`)
- **Live URL:** **https://motazomarien.com** (HTTPS فعّال منذ 2026-09-08 — شهادة Let's Encrypt صدرت وEnforce HTTPS مفعّل)
- **Repo visibility:** **public** (required for Pages on the free plan)
- **Structure:**
  - `content/*.md` — النصوص المنشورة حاليًا على الموقع (الأصل العربي)
  - `translations/{en,es,zh,ru,pt,de}/*.md` — **ترجمات النصوص المنشورة** (إنكليزي/إسباني/صيني/روسي/برتغالي/ألماني)، بنفس تنسيق الترويسة ونفس `slug`. لا تُعرض ترجمة إلا إذا كان أصلها منشورًا في `content/`. **الروسية أُضيفت 2026-09-13** (ترجمات النصوص المنشورة). **البرتغالية أُضيفت 2026-09-14** (ترجمات النصوص التسعة المنشورة). **الألمانية أُضيفت 2026-09-15** (ترجمات النصوص العشرة المنشورة).
  - `queue/*.md` — **طابور النشر المجدول**: نصوص معتمدة تنتظر يومها (بقرار الكاتب 2026-09-06: الاعتماد هنا، والنشر نصًا واحدًا يوميًا بجدول — لا فور الاعتماد). **الطابور عربي فقط ولا يُترجم قبل النشر.**
  - `build.py` — يبني الموقع من `content/` + `translations/`: `python3 build.py` (Python stdlib فقط). كل اللغات تُبنى معًا دائمًا. يولّد أيضًا `sitemap.xml` و`feed.xml` (RSS عربي لآخر 20 نصًا — لحملة البريد اليومية). **يدعم الشعر:** السطر المنفرد داخل الفقرة يتحول إلى `<br>` (أُضيف 2026-09-08 لقصيدة خ-206 — الفقرات النثرية سطر واحد فلا تتأثر). **طبقة التفاعل:** زر مشاركة واتساب يظهر دائمًا تحت كل نص (برابط `https`)، ومعه **زر «انسخ الرابط»** (Clipboard API، يظهر بالسكربت فقط)؛ نموذج التعليقات (Web3Forms → بريد الكاتب) **مفعّل منذ 2026-09-09** ويحوّل بعده إلى **`thanks.html`** لكل لغة (حقل `redirect`)؛ نموذج الاشتراك البريدي (Mailchimp) **مفعّل منذ 2026-09-09** (يظهر في الرئيسية وbook.html بكل اللغات) و**يرسل داخل إطار مخفي** (`mc_frame`) فيبقى القارئ في الموقع ويظهر **تأكيد محلي** (`subscribe-thanks`) بدل صفحة Mailchimp الخام — أُضيف 2026-09-14؛ زر قناة واتساب يظهر فقط بعد لصق قيمته في الثابت `WHATSAPP_CHANNEL_URL` أعلى build.py ثم إعادة البناء؛ وبالطريقة نفسها يظهر زر متابعة يوتيوب في channel.html بعد لصق `YOUTUBE_CHANNEL_URL` (أُضيف 2026-09-21). **شريط تنقّل «الكتاب · الخدمات»** تحت التاجلاين في الترويسة (`site_nav()` في build.py + `{{pagenav}}` في base.html) منذ 2026-09-21.
  - **النشر على Pages:** legacy **«Deploy from a branch»** (main، /root) — كل push يُشغّل بناء تلقائيًا. درس 2026-09-09: المصدر انقلب بصمت إلى «GitHub Actions» (على الأرجح أثناء إصلاح HTTPS يوم 09-08) فتوقفت الدفعات يومًا كاملًا وظهرت 404؛ أعاد الكاتب المصدر إلى branch من إعدادات المستودع (Settings ← Pages) وعاد البناء فورًا. إن توقفت الدفعات مجددًا افحص هذا الإعداد أولًا. (أُضيف مؤقتًا workflow نشر ثم حُذف نفس اليوم — لا workflow في المستودع.)
  - `templates/base.html` + `assets/style.css` — القالب والتصميم (ورقي؛ Amiri للعربية — متنًا وعناوين نصوص — Aref Ruqaa لاسم الموقع وعنوان الكتاب فقط، EB Garamond للإنكليزية/الإسبانية/البرتغالية/الألمانية، Noto Serif SC للصينية، PT Serif للروسية؛ RTL للعربية فقط؛ **line-height 2.2 للمتن العربي منذ 2026-09-10 — تهوية النص المشكول**)
  - `assets/brand/` — الهوية البصرية: `logo.png` (الشعار الكامل الشفاف)، `monogram.png` (المونوغرام المربع: أيقونة المتصفح + ترويسة الموقع)، `og.png` (الشعار على خلفية ورقية لمشاركات التواصل og:image)
  - `robots.txt` + `sitemap.xml` — للزحف والفهرسة (sitemap تولّدها build.py بكل اللغات مع hreflang)
  - **بنية الجناحين (2026-09-22 — قرار الكاتب):** جذر الموقع = **جناح الألعاب** (`index.html` إنكليزي + `bs.html`/`fr.html`/`de.html`/`ar.html` + `assets/games.css` + `404.html` يحوّل العناوين القديمة إلى `/writing/`)، و**جناح الكتابة كله تحت `writing/`** — وهو الموقع الأدبي كما كان بتصميمه الأصلي (بلا طبقة خدمات).
  - **صفحة الخدمات `web.html` (2026-09-25 — بأمر الكاتب «طبق وارفع»):** صفحة إنكليزية في الجذر لعرض خدمات إنشاء المواقع: Hero + «ما أبنيه» (٦ خدمات) + «أعمال مختارة» (Talaix · Talaiz · هذا الموقع) + «كيف أعمل» + «القدرات التقنية» + بريد التواصل. يدخلها **زرّ صغير `Build your site`** في ترويسة صفحات جناح الألعاب الخمس (نمط `.svc-link` في `assets/games.css`، وفي `web.html` بحالة `active`). الصفحة **مكتوبة يدويًا** (لا يبنيها `build.py`) و`noindex` مثل بقية الجناح وغير مُدرجة في `sitemap.xml` — تُفهرس بإدراجها في `GAMES_FILES`/sitemap وإزالة وسم `noindex` عند طلب الكاتب.
  - `writing/index.html` — **واجهة الكتاب: يعرض أحدث نص كاملًا مباشرة + زر كبير «تصفّح الكتاب»**
  - `book.html` — فهرس الكتاب (كل النصوص بتواريخها)
  - `thanks.html` — **صفحة شكر** بعد إرسال التعليق (تولّدها build.py لكل لغة، ويشير إليها حقل `redirect` في نموذج التعليقات؛ غير مدرجة في sitemap)
  - **طبقة الخدمات أُلغيت (2026-09-22):** بناءً على قرار الكاتب «الكتابة تظهر فقط في الكتابة، من دون يوتيوب أو خدمات أو المنصات الأخرى» — حُذفت `services/mediation/channel` من كل اللغات ومن `build.py` (دالتا `hub_page`/`build_hub_pages` لم تعودا موجودتين)، وشريط التنقّل في الكتابة صار «↩ الرئيسية · الكتاب». المنصّتان Talaix/Talaiz تبقىان موقعين مستقلّين خارج الموقع.
  - `pieces/*.html` — صفحات النصوص مع أزرار تنقل كبيرة (السابق/الكتاب/التالي). سطر الميتا يعرض «لقطة N» (رقم النشر التسلسلي) — **أرقام خ داخلية فقط ولا تظهر في الواجهة** (قرار الكاتب 2026-09-07)
  - `series/*.html` — **صفحات السلاسل (الفصول)** — صفحة لكل سلسلة تعرض نصوصها فقط (أُضيفت 2026-09-20 بقرار الكاتب). اسم السلسلة في `book.html` وصفحات القطع والرئيسية صار **رابطًا** إلى صفحته؛ الـ slugs في `SERIES_SLUGS` أعلى build.py (ASCII) وتُبنى لكل لغة (`<lang>/series/`) وتُدرج في `sitemap.xml` مع hreflang.
  - `en/ es/ zh/ ru/ pt/ de/` — نفس البنية مولّدة لكل لغة (index + book + pieces + series)، مع محوّل لغات في الترويسة ووسوم hreflang
  - المخرجات مولّدة — لا تُحرَّر يدويًا، أعد البناء
- **Publishing queue:** النشر من الطابور نص واحد يوميًا بجدول (النشر يحتاج جلسة وكيل للترجمة — يُطلب بـ«انشر نص اليوم» في أي جلسة). **قرار الكاتب 2026-09-10: إعادة تشكيل كل النصوص (المنشور والطابور) تشكيلًا كاملًا — واحدًا واحدًا بترتيب يختاره الكاتب؛ هو يشكّل ويلصق، والوكيل يتحقق من التطابق اللفظي مع المعتمد ثم build + push (الترجمات لا تتأثر — المعنى ثابت). أُنجز: خ-186 (طابور 09-23)، خ-162 (09-12 — بعنوانه المشكول «رُوحُكَ تَموتُ» — **نُشر 09-12**)، خ-096 (09-10 — بعنوانه المشكول «سَرابٌ بِسَرابٍ بِسَرابٍ»)، خ-102 (09-10 — بعنوانها المشكول «مُشاكَسَةٌ مَوريتانِيَّةٌ» — قراءة الكاتب حذفت ال التعريف فصار نكرةً وصفةً)، خ-103 (09-10 — بعنوانها المشكول «لِي صَديقٌ»)، خ-150 (09-10 — بعنوانها المشكول «عَلاءُ…»؛ **درس: أداة التشكيل الآلية تدخّلت في 3 كلمات — أُبقيت صيغ المعتمد: «قولتي»، «ما أزال أتذكر»، «بشرًا ماتوا… روحًا» — بانتظار حسم الكاتب**)، خ-142 (09-10 — بعنوانها المشكول «نِزارُ طَه حاج أَحْمَد»؛ الأداة غيّرت «القارس»←«القارص» — أُبقيت صيغ المعتمد، بانتظار الحسم)، خ-091 (09-10 — بعنوانه المشكول «طَيْرَةُ حَيْفا» — **نُشر 09-11**؛ تطابق لفظي، فرق واحد «أراضٍ»←«أَراضِيَ» أُبقي كما سلّمه الكاتب)، خ-178 (09-13 — بعنوانه المشكول «أَوَّلُ يَوْمٍ فِي الثَّوْرَةِ» — تطابق لفظي، 3 فروق أُبقيت كما سلّمها الكاتب: «ولكنها»→«ولكن»، و«بأنني… بأني»→«أنني… بأنني»؛ أعاد تقسيمه إلى 11 فقرة)، خ-179 (09-14 — بعنوانه المشكول «خُذْ هُوِيَّتَكَ وَارْجِعْ إِلى بَيْتِكَ» — تطابق لفظي، فرقان أُبقيا كما سلّمهما الكاتب: «يعبر عنها بلحظات عمري»→«يعبر عن لحظات عمري»، و«معتز»→«مُعْتَزًّا» (⚠️ اسم→حال — تغيّر دلالي، بانتظار تأكيد الكاتب)؛ أعاد تقسيمه إلى 10 فقرات)، خ-196 (09-15 — بعنوانه المشكول «تَجْرِبَةُ اللُّجُوءِ في فِنْلَنْدا» — تطابق لفظي، فرقان أُبقيا كما سلّمهما الكاتب: «والبطاطس»→«والبَطاطا»، و«بالكردي»→«بالكُرديَّة»؛ أعاد تقسيمه إلى 9 فقرات)، خ-180 (09-16 — بعنوانه المشكول «سَلْوَى» — تطابق لفظي، فرقان أُبقيا كما سلّمهما الكاتب: «أنجاني»→«أَنْقَذَنِيَ»، و«قتلت»→«قَتَلَ»)، خ-182 (09-17 — بعنوانه المشكول «قِصَّةُ الحُبِّ مِن آيندهوفن» — تطابق لفظي، فرق واحد أُبقي كما سلّمه الكاتب: «للحظة»→«لِحَظَةً»؛ مطابق في التقسيم)، خ-211 (09-18 — بعنوانه المشكول «الْمُهَاجِرُ» — تطابق لفظي، إضافة واحدة أُبقيت كما سلّمها الكاتب: «خارج التصنيف»→«خارجٌ عن التصنيف»؛ مطابق في التقسيم)، خ-206 (09-19 — بعنوانها المشكول «سَلْوَى — القَصِيدَةُ» — **تطابق تام** بلا أي فرق لفظي؛ أُعيد تقسيم فقرة نثرية واحدة إلى فقرتين، وبقيت أسطر الشعر كما هي)، خ-145ب (09-20 — بعنوانه المشكول «إِلَى سَارَة» — تطابق لفظي، فرق واحد أُبقي كما سلّمه الكاتب: «معتز»→«مُعْتَزًّا» — **وهذا يرجّح حسم مسألة خ-179: الكاتب يكتب اسمه «مُعْتَزًّا» بصيغة الحال** — **نُشر 2026-09-20**)، خ-191 (09-21 — بعنوانه المشكول «دُعَاء» — **تطابق تام** بلا أي فرق لفظي، والتقسيم مطابق — **نُشر 2026-09-21**)، خ-193 (09-22 — بعنوانه المشكول «أَنَا مَخْلُوقٌ مِنْ نَدَمٍ» — **تطابق تام** بلا أي فرق لفظي؛ آخر عنصر في الطابور).** الجدول: خ-091 «طيرة حيفا» **نُشر 2026-09-11** ✅ (سلسلة «الغربة اليومية» — بترجماتها الثلاث) — خ-162 «روحك تموت» **نُشر 2026-09-12** ✅ (سلسلة «يوميات المهاجر» — بترجماتها الثلاث؛ أول ظهور للسلسلة مترجمة: en «Diaries of the Migrant»، es «Diarios del emigrante»، zh «移民日记») — خ-178 «أول يوم في الثورة» **نُشر 2026-09-13** ✅ (سلسلة «ما قبل الرحيل» — بترجماته الثلاث + الروسية)، خ-179 «خذ هويتك وارجع إلى بيتك» **نُشر 2026-09-14** ✅ (سلسلة «ما قبل الرحيل» — بترجماته الخمس)، خ-196 «تجربة اللجوء في فنلندا» **نُشر 2026-09-15** ✅ (سلسلة «يوميات المهاجر» — بترجماته الست؛ والألمانية أُضيفت للموقع اليوم نفسه)، خ-180 «سلوى» **نُشر 2026-09-16** ✅ (سلسلة جديدة «ما بقي» — بترجماته الست؛ أول ظهور للسلسلة مترجمة: en «What Remains»، es «Lo que queda»، zh «所余»، ru «Что осталось»، pt «O que resta»، de «Was bleibt» — **أُعيدت تسمية السلسلة 2026-09-18 بقرار الكاتب: كانت «سلوى والحب»، وصارت «ما بقي» — اسم موضوعي عميق لفصل الحب كله، لا يحمل اسمًا شخصيًا (سلوى اسم بطلة القوس فقط)**)، خ-182 «قصة الحب من آيندهوفن» **نُشر 2026-09-17** ✅ (سلسلة «ما بقي» — بترجماته الست)، خ-211 «المهاجر» **نُشر 2026-09-18** ✅ (سلسلة «يوميات المهاجر» — نص العنوان للمشروع)، خ-206 «سَلْوَى — القَصِيدَةُ» **نُشر 2026-09-19** ✅ (سلسلة «ما بقي» — أول قصيدة)، خ-145ب «إلى سارة» **نُشر 2026-09-20** ✅ (سلسلة «ما بقي» — خاتمة القوس، بترجماته الست)، خ-191 «دعاء» **نُشر 2026-09-21** ✅ (سلسلة «ما بقي»)، خ-193 «أنا مخلوق من ندم» يوم 2026-09-22 (سلسلة «ما بقي» — معتمدة 09-10). **ومن 09-23 يحكم الجدولَ `wr/تطوير/خطة-التنفيذ-اليومية.md`:** بقية نصوص «المهاجر» الـ17 بترتيب الأبواب، ثم **«بُرهان» (خ-152 — معتمدة 09-09) ٥ مقاطع 2026-10-10 ← 10-14**. عند النشر: `git mv queue/… content/…` + تحديث date + **إضافة ترجماته الست إلى `translations/{en,es,zh,ru,pt,de}/<slug>.md` بجودة أدبية عالية** + build + push. (نص بلا ترجمات يظهر عربيًا فقط حتى تُضاف.)
- **HTTPS:** **resolved 2026-09-08** — Let's Encrypt cert active (apex+www), Enforce HTTPS on, watcher self-deleted. Details in PLAN.md.
- **Book being serialized:** «المهاجر — لقطات ومرايا» (الكتاب الأول، خطة `wr/تطوير/خطة-كتاب-المهاجر.md`) — بقرار الكاتب 2026-09-06. قرارات الشكل: النص الأخير على الرئيسية، أزرار كبيرة واضحة، التبحر داخل الكتاب عبر book.html. إيقاع النشر: **طابور مجدول — نص واحد يوميًا** (انظر Publishing queue). الكتب التالية موثقة في `wr/تطوير/الخطة-المتكاملة.md` (رسائل بين، لبن، مقالات) — نبدأ بها بعد انتهاج «المهاجر».
- **Commands:** بعد إضافة/تعديل ملف في `content/`: `python3 build.py && git add -A && git commit && git push`
- **Libraries:** none (plain HTML/CSS, Python stdlib build)
- **Content source:** النصوص من ورشة الكتابة `/Users/digital-inclusion/Documents/work/wr` — يُنشر فقط ما يوافق عليه الكاتب بموافقة نهائية موثقة (درس خ-031). **المنشور الآن:** خ-121 «الطائِرَة: وُجوه» (2026-09-26، بترجماته الست؛ سلسلة «وجوه المهاجر» — مشكول بتشكيل الوكيل). **في الطابور:** خ-169 «خلف النوافذ» (09-27 — «العبور» — مشكول بتشكيل الوكيل). ملاحظة تقنية: ترتيب النصوص يُحسم برقم خ عند تساوي التاريخ.
- **Domain/DNS:** custom domain `motazomarien.com` (CNAME file in repo). Registrar + DNS: **Regery** (الهجرة من Virtono اكتملت 2026-09-05؛ لم يعد لـ Virtono أي صلة بالنطاق). Zone keeps GitHub A records + www CNAME + google-site-verification TXT + **ImprovMX mail records (أُضيفت 2026-09-09): MX @ ← mx1.improvmx.com (10) وmx2.improvmx.com (20)، TXT @ ← `v=spf1 include:spf.improvmx.com ~all`** + **DKIM لـ Mailchimp (أُضيفا 2026-09-09): CNAME k1._domainkey وk2._domainkey ← dkim.mcsv.net** + **DMARC (أُضيف 2026-09-14): TXT `_dmarc` ← `v=DMARC1; p=none`**. Mail: **`motaz@motazomarien.com` يُحوَّل عبر ImprovMX (مجاني) إلى Gmail الشخصي motaz3d@gmail.com** — استقبال فقط؛ الغرض: توثيق النطاق في Mailchimp وعنوان مرسل احترافي للنشرة. **درس 2026-09-14:** يجب وجود alias صريح `motaz` ← `motaz3d@gmail.com`؛ الـ catch-all وحده إذا وُجّه إلى عنوان على النطاق نفسه يُنشئ حلقة تُسقط البريد (سبب تعذّر وصول رسائل تحقق Mailchimp). User does NOT use Zoho.
