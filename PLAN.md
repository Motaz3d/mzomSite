# PLAN.md — Work status

## Done
- Project folder created, AGENTS.md session protocol established, repo pushed to GitHub.
- Placeholder homepage (index.html, Arabic RTL) created and pushed.
- GitHub Pages enabled on branch `main` (path `/`) — live at https://motaz3d.github.io/mzomSite/
- Repo made public (required for Pages on the free plan).
- CNAME file created with `motazomarien.com`; custom domain set in Pages (status: building).

## In progress
- DNS at **Virtono** (the active zone; Regery DNS is ignored): user must add GitHub records.
  - Current live DNS: apex A → 146.70.56.163, www CNAME → apex. GitHub records NOT yet added there.

## Done (additions)
- DNS migration complete: NS switched from Virtono to **Regery NS** (ns-cloud-e*.googledomains.com). Virtono fully detached from the domain.
- Stale records (old server A/AAAA, all Zoho records) deleted from Regery zone by the user; only GitHub records remain.
- Domain verified live: apex → 4 GitHub IPs, www → motaz3d.github.io; http://motazomarien.com serves the site (200).

## Done (additions)
- Literary site built: `build.py` (stdlib) renders `content/*.md` → `index.html` + `pieces/*.html`, paper-style Arabic RTL design.
- First piece published: خ-031 «رسائل إلى مارينا — 1: اللسان» — **then unpublished the same evening at the author's request** (not satisfied with it). Site currently shows an empty state ("قريبًا"); build.py handles zero pieces.

## Done (additions)
- Multilingual site: خ-102 translated into English («Sparring with the Mauritanian»), Spanish («Piques con el mauritano»), Chinese (《与毛里塔尼亚人的斗嘴》) — literary-grade, done by the agent 2026-09-06 at the author's request ("نفذ واظهر على الموقع").
- build.py rebuilt multilingual: `translations/{en,es,zh}/*.md` → `en/ es/ zh/` (index + book + pieces per language), language switcher in header, hreflang alternates, per-language fonts (EB Garamond, Noto Serif SC) and dates. Arabic unchanged at root.
- SEO/brand (2026-09-06): Google Search Console verified via TXT record at Regery (keep the record). `sitemap.xml` generated by build.py (live, 200); submitting it in Search Console failed with "Invalid sitemap address" because Google fetches via HTTPS and cert is pending — **resubmit after HTTPS is on**. `robots.txt` live. Logo deployed: monogram in site header + favicon, `assets/brand/og.png` as og:image on all pages.

## Done (additions)
- خ-103 «لي صديق» published 2026-09-07: `git mv queue→content`, date → 09-07, literary translations added (en «A Friend of Mine», es «Un amigo mío», zh 《我有一个朋友》), rebuilt all languages, pushed.
- Interaction layer (2026-09-07): WhatsApp share button live under every piece (all 4 languages, works now); `feed.xml` RSS (Arabic, last 20 pieces, valid XML) generated by build.py + RSS link tag in head; comment form (Web3Forms → author's Gmail, name+email+comment, hidden until key pasted) and newsletter subscribe form (Mailchimp action) + WhatsApp channel button — all gated on the 3 constants at the top of build.py, so nothing broken shows before activation. CSS added (paper style).
- UI humanization (2026-09-07, author's request): internal workshop numbers (خ-XXX) removed from the public UI — piece meta now shows date + «لقطة N» (publication order; en "Snapshot N", es "Instantánea N", zh "剪影 N", matching each book title). kh numbers remain internal only (queue filenames, sorting, workshop). `fmt_kh` deleted from build.py.
- خ-178 «أول يوم في الثورة» **approved by the author 2026-09-07** (reviewed via the new browser-page ritual: `wr/تطوير/عروض-مراجعة/خ-178.html`; proposal in `wr/تطوير/نماذج/خ-178-اول-يوم-في-الثورة.md` — 14 dictation fixes; approval covered keeping the «علويًا» perception line, keeping the «مطارد» disclosure, series «ما قبل الرحيل») → `queue/kh-178-awwal-yawm-thawra.md`, publishes 2026-09-13.

## Done (additions)
- **HTTPS resolved 2026-09-08:** Let's Encrypt cert issued ~15:52 UTC (covers apex + www, expires 12-07) after the 09-07 support ticket + clean remove/re-add. `https_certificate.state: approved`. **Enforce HTTPS enabled via API 18:55** (`https_enforced: true`, site `built`); https://motazomarien.com serves 200, www→apex 301s correct. Watcher `com.motazomarien.httpscheck` detected success and self-deleted (plist removed, agent unloaded). Apex http→https edge redirect still rolling out (GitHub allows up to 24h after enforcement). **Unblocked follow-ups:** resubmit sitemap in Google Search Console (was "Invalid sitemap address" over broken HTTPS); Mailchimp RSS campaign on `https://motazomarien.com/feed.xml` now fetchable. GitHub support ticket can be closed as resolved.

## Done (additions)
- خ-142 «نزار طه حاج أحمد» published 2026-09-09 (series «وجوه المهاجر», en/es/zh translations).
- **Pages deploy fixed 2026-09-09:** build_type had silently flipped to `workflow` (likely during the 09-08 HTTPS remove/re-add), so no deploys ran after 99eb13d — yesterday's approvals and خ-142 never went live. PATCH back to `legacy` 404'd via API, so a temporary Actions deploy workflow was added — then the **user re-switched Source to «Deploy from a branch» in Settings ← Pages** (11:04), legacy builds resumed instantly, and the workflow file was removed. Final state: legacy branch deploys, as before. Lesson: if deploys stop, check Settings ← Pages source first.
- **Comments activated 2026-09-09:** Web3Forms key pasted into `WEB3FORMS_ACCESS_KEY` (build.py:43); comment form now renders under every piece in all 4 languages (submissions → author's Gmail).

## Done (additions)
- خ-096 «سَرابٌ بِسَرابٍ بِسَرابٍ» published 2026-09-10 (series «العبور» — new series: en "The Crossing", es "La travesía", zh 渡河; translations: en «Mirage Upon Mirage Upon Mirage», es «Espejismo tras espejismo tras espejismo», zh 《蜃楼复蜃楼又蜃楼》). **أُعيد نشرها مشكولةً مساء اليوم نفسه** — شكّلها الكاتب بيده وألصقها (تطابق لفظي كامل مع المعتمد، تشكيل فقط؛ الترجمات لم تُمسّ) — أول منشور يُعاد تشكيله ضمن مشروع التشكيل. ومساء اليوم أيضًا: **خ-102 «مشاكسة الموريتانيّة» أُعيد نشرها مشكولةً** — تطابق لفظي في المتن، والعنوان بقراءة الكاتب المشكولة «مُشاكَسَةٌ مَوريتانِيَّةٌ» (حذف ال التعريف — نكرةً وصفةً).

## Next
- Daily publishing: cron `01M1V0B9MRNN8JF42DZB7WRJ08` died with the 09-06 morning session — **no live scheduler now**. Publishing needs the agent (literary translation ×3), so it can't be a plain OS timer. خ-103 published 09-07 ✓; خ-150 published 09-08 ✓; خ-142 published 09-09 ✓; خ-096 published 09-10 ✓ (series «العبور», en/es/zh translations added). To publish the day's piece (خ-091 on 09-11, خ-162 on 09-12): open any session here and say «انشر نص اليوم».
- **مشروع التشكيل (قرار الكاتب 2026-09-10 — التفاصيل وطريقة العمل في AGENTS.md ← Publishing queue):** أُنجز 12/16 — المنشورات الخمس كلها (خ-102، خ-103، خ-150، خ-142، خ-096) + خ-186 وخ-091 وخ-162 وخ-178 وخ-179 وخ-196 وخ-180 في الطابور — **شُكّل عنوان خ-096 أيضًا (09-10): «سَرابٌ بِسَرابٍ بِسَرابٍ».** الباقي: عناصر الطابور الأربعة خ-182 ← … ← خ-193. **معلّق بانتظار حسم الكاتب — 4 كلمات غيّرتها أداة التشكيل الآلية وأُبقيت صيغ المعتمد:** ① «قولتي» ② «ما أزال أتذكر» (خ-150) ③ «بشرًا ماتوا… روحًا» (خ-150) ④ «القارس» (خ-142) — «كما هي» تُحسمها أو رقمها للتبديل. خ-091 (09-11) ✅ مشكولة (09-10 — تطابق لفظي؛ فرق واحد «أراضٍ»←«أَراضِيَ» أُبقي كما سلّمه الكاتب). خ-162 (09-12) ✅ مشكولة (09-10 — تطابق لفظي؛ فرقان أُبقيا كما سلّمهما الكاتب: «شنتها»→«شانتها» حرفيًّا، و«ابنًا»→«ابنٍ» إعرابًا). خ-178 (09-13) ✅ مشكولة (09-10 — تطابق لفظي؛ 3 فروق أُبقيت كما سلّمها الكاتب: «ولكنها»→«ولكن»، و«بأنني… بأني»→«أنني… بأنني»؛ أعاد تقسيم النص إلى 11 فقرة بدل 3). خ-179 (09-14) ✅ مشكولة (09-10 — تطابق لفظي؛ فرقان أُبقيا كما سلّمهما الكاتب: «يعبر عنها بلحظات عمري»→«يعبر عن لحظات عمري»، و«معتز»→«مُعْتَزًّا» — ⚠️ تغيّر دلالي (اسم→حال)، بانتظار تأكيد الكاتب؛ أعاد تقسيم النص إلى 10 فقرات بدل 2). خ-196 (09-15) ✅ مشكولة (09-10 — تطابق لفظي؛ فرقان أُبقيا كما سلّمهما الكاتب: «والبطاطس»→«والبَطاطا»، و«بالكردي»→«بالكُرديَّة»؛ أعاد تقسيم النص إلى 9 فقرات بدل 8). خ-180 (09-16) ✅ مشكولة (09-10 — تطابق لفظي؛ فرقان أُبقيا كما سلّمهما الكاتب: «أنجاني»→«أَنْقَذَنِيَ»، و«قتلت»→«قَتَلَ»؛ مطابق في التقسيم). **طباعة المشكول (09-10): line-height 2.2 للمتن العربي + عنوان النص Amiri Bold** (لا Ruqaa للعناوين المشكولة — رسم حركاتها ضعيف).
- User: cancel all active services at Virtono — support email drafted 2026-09-07; user says Virtono has now been contacted (09-07). Await their confirmation reply; back up anything needed before final termination.
- User activation steps for the interaction layer (each = paste the value into build.py constants + `python3 build.py` + push — offer to do the rebuild in-session):
  1. ~~**Comments**~~ — **done 2026-09-09** (Web3Forms key active).
  2. **Daily email:** ~~subscribe form~~ **done 2026-09-09** — Mailchimp audience created, `NEWSLETTER_FORM_ACTION` = post URL derived from the hosted-form share link (eepurl short link → `subscribe?u=…&id=…` → `subscribe/post?…`). Form shows on homepage + book page (all 4 languages); submissions land in the Mailchimp audience directly (single opt-in). **Remaining: RSS campaign** — Campaigns → RSS campaign on `https://motazomarien.com/feed.xml` (daily) so subscribers get the day's text by email automatically. **Daily reminder set:** cron `01M22S3379MB4BP9A395HEPEM5` (09:47 daily) — delete it with CronDelete once the campaign exists.
     - **توثيق النطاق (2026-09-09):** Mailchimp يرفض توثيق `gmail.com` («Error sending verification email — free providers can't be verified»). **الكاتب اختار المسار الاحترافي:** إعادة توجيه بريد على `motazomarien.com` عبر **ImprovMX** (مجاني، نطاق واحد) ← `motaz@motazomarien.com` يحوّل إلى `motaz3d@gmail.com`. **السجلات المطلوب إضافتها في Regery (دون المساس بالموجود):** MX @ ← `mx1.improvmx.com` (10)، MX @ ← `mx2.improvmx.com` (20)، TXT @ ← `v=spf1 include:spf.improvmx.com ~all`. حالة النطاق قبل الإضافة (تحقق 09-09): لا MX، TXT واحد فقط = google-site-verification (يُبقى)، A الأربعة = GitHub. **السجلات الثلاثة أُضيفت في Regery وانتشرت ✓ (تحقق dig 09-09 16:23)، وImprovMX أكّدها: النطاق Active ✓ (09-09 16:36). رسالة توثيق Mailchimp وصلت إلى Gmail عبر التحويل ✓ والنطاق Verified. سجلّا DKIM (CNAME k1/k2._domainkey ← dkim.mcsv.net) أُضيفا وانتشرا ✓ (dig 09-09 18:05).** المتبقي: ضغط Authenticate في Mailchimp للتأكيد ← ثم حملة RSS من motaz@motazomarien.com. **ما زال معلقًا:** خانة «Send a final welcome email» لم يُعثر عليها بعد (الميزة متاحة للمجاني — جرّب: أسفل صفحة Form builder، أو المسار Audience ← Signup forms، أو تحديث الصفحة).
     - **تجربة المشترك (درس 2026-09-09):** أول مشتركَين حقيقيَّين وصلا (نفس الشخص بعنوانَي Gmail من IP واحد بفارق دقيقة) — **الاشتراك نجح** لكنه بدا للمشترك كأنه خطأ: النموذج يفتح صفحة Mailchimp الإنجليزية العارية في تبويب جديد، ولا تصل أي رسالة ترحيب (بريد Mailchimp للكاتب «subscribed but did not receive a welcome email» مجرد تنبيه تسويقي لا خطأ). واجهة `post-json` للرد المضمّن داخل الموقع **مغلقة (404)** — لا يمكن إظهار رد عربي inline بلا خادم. **خطوات الكاتب في لوحة Mailchimp (مجانية):** ① تفعيل «Final welcome email» وكتابة نصها بالعربية (Audience ← Signup forms ← Form builder ← قائمة Forms and response emails ← Final welcome email ← تفعيل Send a final welcome email) ② ضبط اللغة الافتراضية للجمهور عربية (Audience settings) لتخرج صفحات الرد بالعربية. (الـ Welcome automation التي يعلن عنها البريد ميزة مدفوعة غالبًا — لا يلزم؛ رسالة الترحيب الكلاسيكية تكفي.)
  3. **WhatsApp:** create a Channel in the WhatsApp app (Updates ← Channels ← Create) → copy follow link → `WHATSAPP_CHANNEL_URL`; author posts the day's link there manually.
- GitHub support ticket re: stuck HTTPS cert — **resolved 2026-09-08** (cert issued, Enforce HTTPS on; see Done). User can close the ticket as resolved.
- Queue extended to 09-21: **خ-191 «دعاء» approved by the author 2026-09-08** (بكلمة «معتمدة» بعد عرض `wr/تطوير/عروض-مراجعة/خ-191.html`) — حسم: إبقاء «المختار المصطفى»، النشر وحده لقطة، الاسم الصريح → `queue/kh-191-duaa.md` (2026-09-21، «سلوى والحب»). وقبلها بنفس اليوم: خ-179 (09-14)، خ-196 (09-15)، خ-180 (09-16)، خ-182 (09-17)، خ-211 (09-18)، خ-206 (09-19)، خ-145ب (09-20). خ-162 approved 2026-09-07 → 09-12 ✓. **خ-152 «برهان» ✅ اعتمد الكاتب النسخة المشكولة 2026-09-09** — **٥ مقاطع يومية تُنشر 2026-10-10 ← 10-14** خاتمةً للباب الأول (بعد خ-160 يوم 10-08 وخ-140 يوم 10-09)؛ تُجهَّز من النص المعتمد وتُعرض قبل موعدها. **خطة التنفيذ اليومية وُلّدت 2026-09-09 بتفويض الكاتب** (`wr/تطوير/خطة-التنفيذ-اليومية.md`): بقية نصوص «المهاجر» الـ17 بترتيب موثق من 09-23، والكتاب التالي «رسائل إلى بين». **خ-193 «أنا مخلوق من ندم» ✅ اعتمدها الكاتب 2026-09-10** (بكلمة «معتمد» بعد عرض `wr/تطوير/عروض-مراجعة/خ-193.html`) — بلا تنقيح (حُذف العنوان الآلي فقط)؛ الاعتماد شمل: العنوان، سلسلة «سلوى والحب» (تتسع لقوس الباب الخامس كله)، والموعد → `queue/kh-193-makhluq-min-nadam.md` (2026-09-22). **خ-186 «الخوف» ✅ اعتمدها الكاتب 2026-09-10** (بتسليمه النص مشكولًا بيده، وتكرار التسليم بعد سؤال الاعتماد — عُدّ اعتمادًا نهائيًا) → `queue/kh-186-alkhawf.md` (2026-09-23، مشكولة). التالي للمراجعة: خ-209ب (09-24). خ-140 «مريم» ⚠️ حسّاس (§5.4). Review = author session per text (درس خ-031) **presented as a browser page**, then move to `queue/` with a date.
- Build the real site content.
