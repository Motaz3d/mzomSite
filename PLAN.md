# PLAN.md — Work status

## Done
- Project folder created, AGENTS.md session protocol established, repo pushed to GitHub.
- Placeholder homepage (index.html, Arabic RTL) created and pushed.
- GitHub Pages enabled on branch `main` (path `/`) — live at https://motaz3d.github.io/mzomSite/
- Repo made public (required for Pages on the free plan).
- CNAME file created with `motazomarien.com`; custom domain set in Pages (status: building).

## In progress
- DNS at **Virtono** (the active zone; Regery DNS is ignored): user must add GitHub records.
  - Current live DNS: apex A → 146.70.56.163, www CNAME → apex. GitHub records NOT yet added there.

## Done (additions)
- DNS migration complete: NS switched from Virtono to **Regery NS** (ns-cloud-e*.googledomains.com). Virtono fully detached from the domain.
- Stale records (old server A/AAAA, all Zoho records) deleted from Regery zone by the user; only GitHub records remain.
- Domain verified live: apex → 4 GitHub IPs, www → motaz3d.github.io; http://motazomarien.com serves the site (200).

## Done (additions)
- Literary site built: `build.py` (stdlib) renders `content/*.md` → `index.html` + `pieces/*.html`, paper-style Arabic RTL design.
- First piece published: خ-031 «رسائل إلى مارينا — 1: اللسان» — **then unpublished the same evening at the author's request** (not satisfied with it). Site currently shows an empty state ("قريبًا"); build.py handles zero pieces.

## Done (additions)
- Multilingual site: خ-102 translated into English («Sparring with the Mauritanian»), Spanish («Piques con el mauritano»), Chinese (《与毛里塔尼亚人的斗嘴》) — literary-grade, done by the agent 2026-09-06 at the author's request ("نفذ واظهر على الموقع").
- build.py rebuilt multilingual: `translations/{en,es,zh}/*.md` → `en/ es/ zh/` (index + book + pieces per language), language switcher in header, hreflang alternates, per-language fonts (EB Garamond, Noto Serif SC) and dates. Arabic unchanged at root.
- SEO/brand (2026-09-06): Google Search Console verified via TXT record at Regery (keep the record). `sitemap.xml` generated by build.py (live, 200); submitting it in Search Console failed with "Invalid sitemap address" because Google fetches via HTTPS and cert is pending — **resubmit after HTTPS is on**. `robots.txt` live. Logo deployed: monogram in site header + favicon, `assets/brand/og.png` as og:image on all pages.

## Done (additions)
- خ-103 «لي صديق» published 2026-09-07: `git mv queue→content`, date → 09-07, literary translations added (en «A Friend of Mine», es «Un amigo mío», zh 《我有一个朋友》), rebuilt all languages, pushed.

## Next
- Enable "Enforce HTTPS" — cert still pending at GitHub Let's Encrypt (`https_certificate: null`; retriggers: 09-06 ~03:38 and 21:05). Diagnosed 2026-09-06 22:45: **everything our side is correct** — Pages health check green for apex + www (`is_valid`, `is_served_by_pages`, `is_https_eligible`, no CAA/proxy/stale-IP issues), DNS identical on 8.8.8.8/1.1.1.1/9.9.9.9, ACME path reachable on all 4 GitHub IPs; HTTPS still serves generic `*.github.io` cert. GitHub-assistant messages ("bad_content_encoding", "enable Enforce HTTPS") are just symptoms of the unissued cert — the Enforce HTTPS toggle only appears after issuance. Blocked purely on GitHub's issuance queue. **22:55: did the official stuck-cert remedy from GitHub docs — removed the custom domain via API (`PUT pages cname=null`), waited 45s, re-added it. This cancels and restarts provisioning: `https_certificate` flipped from `null` → `"new"` (request freshly queued), site rebuilding, HTTP still 200.** Note: the watcher's plain re-PUT of the same cname (03:38, 21:05) never restarted the job — only remove/re-add does. Session cron `01M1TRNXMSXMEPEYNCWMKHMHAM` died with the 09-06 morning session. Replaced 09-06 21:05 with **launchd agent `com.motazomarien.httpscheck`** (`~/Library/LaunchAgents/com.motazomarien.httpscheck.plist` → `~/scripts/https-cert-check/check.sh`): every 30min checks HTTPS, retriggers cert via API (throttled to once/6h so in-flight issuance isn't reset), on success enables Enforce HTTPS and self-deletes. Runs without any Kimi session (Mac must be awake; log at `~/scripts/https-cert-check/check.log`). If still pending ~24h after the 22:55 restart: GitHub support (human ticket — repo Motaz3d/mzomSite, health check shows `is_https_eligible: true` both hosts).
- Daily publishing: cron `01M1V0B9MRNN8JF42DZB7WRJ08` died with the 09-06 morning session — **no live scheduler now**. Publishing needs the agent (literary translation ×3), so it can't be a plain OS timer. خ-103 published on 09-07 ✓. To publish the day's piece (خ-150 on 09-08, خ-142 on 09-09, خ-096 on 09-10, خ-091 on 09-11): open any session here and say «انشر نص اليوم».
- User: cancel all active services at Virtono — user doesn't know the client area; support email drafted for support@virtono.com (2026-09-07, awaiting user's account details). Safe now that DNS is off Virtono; back up anything needed first.
- Build the real site content.
